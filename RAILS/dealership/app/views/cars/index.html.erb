<h2 id="listings">Our current listings:</h2>

<% @cars.each do |car| %>
<article>
    <header>
        <h2 id="car_listing"><%= link_to("#{car.year} #{car.make} #{car.model}",
         car_path(car)) %></h2>
    </header>
</article>
<% end %>
